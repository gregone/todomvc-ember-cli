import Ember from 'ember';

export default Ember.Route.extend({
  // Todo: Add a localStorage adapter to avoid Ember Data errors
  // model: function(){
  //   return this.store.find('todo');
  // }
});
